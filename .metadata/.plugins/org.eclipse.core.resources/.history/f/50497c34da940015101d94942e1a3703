package com.cnkvha.uuol.sjl.db;

public final class SQLHelper {
	public static String encodeField(String field, int obj, boolean isNumber){
		return "\"" + field + "\"=" + (isNumber ? "" : "") + obj.toString();
	}
}
